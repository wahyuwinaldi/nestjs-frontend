-- ================================================================
-- Seed: 99_sequences.sql
-- No domain-specific sequences remain after stripping domain modules.
-- ================================================================
SET search_path TO public;

-- Placeholder: add setval() calls here when new SERIAL sequences are introduced.
SELECT 1;
